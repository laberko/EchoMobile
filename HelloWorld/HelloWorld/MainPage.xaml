<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:local="clr-namespace:HelloWorld;assembly=HelloWorld"
             x:Class="HelloWorld.MainPage">
  
  <StackLayout Padding="10, 0" BackgroundColor ="{Binding Colour}" HorizontalOptions="FillAndExpand">
    <StackLayout.BindingContext>
      <local:ClockViewModel />
    </StackLayout.BindingContext>
  
  <Label Text="{Binding DateTime, StringFormat='{0:HH:mm:ss}'}"
         TextColor ="{Binding TextColour}"
         FontSize = "36"
         HorizontalOptions="Center"
         VerticalOptions="Center">
    <Label.BindingContext>
      <local:ClockViewModel />
    </Label.BindingContext>
  </Label>
  
</StackLayout>

</ContentPage>